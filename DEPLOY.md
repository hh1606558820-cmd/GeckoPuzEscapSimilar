# 部署指南

本指南将帮助你将这个关卡编辑器部署到网上，让其他人可以直接在浏览器中使用。

## 🚀 快速开始

### 方法一：GitHub Pages（免费，推荐）

GitHub Pages 是最简单的免费托管方案。

#### 步骤：

1. **构建项目**
   ```bash
   npm run build
   ```

2. **上传到 GitHub**
   - 在 GitHub 创建新仓库
   - 将项目代码推送到 GitHub

3. **配置 GitHub Pages**
   - 进入仓库的 Settings → Pages
   - Source 选择 "GitHub Actions"（推荐）或 "Deploy from a branch"
   - 如果选择 branch，选择 `gh-pages` 分支，目录选择 `/ (root)`
   - 如果选择 GitHub Actions，使用提供的 workflow 文件（见 `.github/workflows/deploy.yml`）

4. **访问网站**
   - GitHub 会提供一个地址：`https://你的用户名.github.io/仓库名/`

#### 自动化部署（推荐）

使用提供的 GitHub Actions 工作流（`.github/workflows/deploy.yml`），每次推送到 `main` 分支时会自动构建并部署。

---

### 方法二：Netlify（免费，最简单）

Netlify 提供最简单的部署体验。

#### 步骤：

1. **构建项目**
   ```bash
   npm run build
   ```

2. **部署到 Netlify**
   - 访问 [netlify.com](https://www.netlify.com)
   - 注册/登录账号
   - 点击 "Add new site" → "Import an existing project"
   - 连接到你的 GitHub 仓库
   - 配置构建设置：
     - Build command: `npm run build`
     - Publish directory: `dist`
   - 点击 "Deploy site"

3. **访问网站**
   - Netlify 会提供一个随机域名，如：`https://random-name-123.netlify.app`
   - 可以在 Site settings 中自定义域名

#### 使用 Netlify CLI（可选）

```bash
# 安装 Netlify CLI
npm install -g netlify-cli

# 登录
netlify login

# 部署
npm run build
netlify deploy --prod --dir=dist
```

---

### 方法三：Vercel（免费，适合前端项目）

Vercel 对前端项目非常友好。

#### 步骤：

1. **部署到 Vercel**
   - 访问 [vercel.com](https://vercel.com)
   - 注册/登录账号
   - 点击 "Add New Project"
   - 导入你的 GitHub 仓库
   - Vercel 会自动检测 Vite 项目，无需配置
   - 点击 "Deploy"

2. **访问网站**
   - Vercel 会提供一个域名，如：`https://your-project.vercel.app`

#### 使用 Vercel CLI（可选）

```bash
# 安装 Vercel CLI
npm install -g vercel

# 部署
vercel

# 生产环境部署
vercel --prod
```

---

### 方法四：本地服务器（测试用）

如果你想在本地测试构建后的版本，或者通过局域网分享：

#### 使用 Vite Preview（推荐）

```bash
npm run build
npm run preview
```

#### 使用 Python 简单服务器

```bash
npm run build
cd dist
# Python 3
python -m http.server 8000
# Python 2
python -m SimpleHTTPServer 8000
```

然后在浏览器访问 `http://localhost:8000`

#### 使用 Node.js 服务器

```bash
npm run build
npx serve dist -l 8000
```

---

### 方法五：使用提供的本地服务器脚本

运行 `serve.bat`（Windows）或 `serve.sh`（Linux/Mac）脚本：

```bash
# Windows
serve.bat

# Linux/Mac
chmod +x serve.sh
./serve.sh
```

---

## 📦 构建说明

所有部署方法都需要先构建项目：

```bash
# 安装依赖（首次运行）
npm install

# 构建生产版本
npm run build
```

构建完成后，`dist` 目录包含所有静态文件，可以直接部署。

---

## 🔧 配置说明

### Vite 配置

`vite.config.ts` 中已配置：
- `base: './'` - 使用相对路径，支持部署到子目录
- 这意味着可以部署到 `https://example.com/subfolder/` 这样的路径

如果需要部署到根目录，可以修改为：
```typescript
base: '/'
```

### 自定义域名

大多数平台都支持自定义域名：
- **GitHub Pages**: Settings → Pages → Custom domain
- **Netlify**: Site settings → Domain management
- **Vercel**: Project Settings → Domains

---

## 🎯 推荐方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **GitHub Pages** | 免费、与代码仓库集成、支持自定义域名 | 需要 GitHub 账号 | 已有 GitHub 仓库 |
| **Netlify** | 部署简单、自动 HTTPS、CDN 加速 | 免费版有构建时间限制 | 快速部署、自动化 |
| **Vercel** | 对前端友好、全球 CDN、自动优化 | 需要注册账号 | React/Vue 等前端项目 |
| **本地服务器** | 无需账号、完全控制 | 需要手动运行、只能局域网访问 | 测试、内部分享 |

---

## 📝 注意事项

1. **构建命令**：所有平台都需要运行 `npm run build`
2. **输出目录**：构建后的文件在 `dist` 目录
3. **路径问题**：当前配置使用相对路径（`base: './'`），支持部署到任意路径
4. **环境变量**：如果项目使用环境变量，需要在部署平台配置
5. **HTTPS**：所有主流平台都自动提供 HTTPS

---

## 🆘 常见问题

### Q: 部署后页面空白？
A: 检查浏览器控制台错误，可能是路径配置问题。确保 `vite.config.ts` 中 `base` 配置正确。

### Q: 如何更新部署的网站？
A: 
- **GitHub Pages (Actions)**: 推送代码到 GitHub 自动部署
- **Netlify/Vercel**: 推送代码自动触发部署，或手动重新部署

### Q: 可以部署到自己的服务器吗？
A: 可以。将 `dist` 目录的内容上传到你的 Web 服务器（如 Nginx、Apache）即可。

---

## 🔗 相关资源

- [Vite 部署指南](https://cn.vitejs.dev/guide/static-deploy.html)
- [GitHub Pages 文档](https://docs.github.com/zh/pages)
- [Netlify 文档](https://docs.netlify.com/)
- [Vercel 文档](https://vercel.com/docs)




